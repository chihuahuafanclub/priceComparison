# Frontend (React)

這是 PriceComparison 的前端專案，使用 Create React App 建置。

完整專案文件請先看：

- [../README.md](../README.md)

## 常用指令

```bash
npm install
npm start
npm run build
```

## 開發預設 API

在本機開發時，前端會預設打到：

- `http://localhost:8000`

如需改成其他 API 網址，可設定環境變數：

- `REACT_APP_API_BASE_URL`
